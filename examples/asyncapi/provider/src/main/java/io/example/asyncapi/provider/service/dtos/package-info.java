/** Inbound DTOs. */
@keep
package io.example.asyncapi.provider.service.dtos;

@interface keep {
    // keeps this package even when it's empty,
    // allowing wildcard import "io.example.asyncapi.provider.core.inbound.dtos.*;"
}
