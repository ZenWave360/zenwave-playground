/** Inbound DTOs. */
@keep
package io.example.asyncapi.shoppingcart.dtos;

@interface keep {
    // keeps this package even when it's empty,
    // allowing wildcard import "io.example.asyncapi.shoppingcart.core.inbound.dtos.*;"
}
